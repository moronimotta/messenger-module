# ...existing code...
PORT=8080

# Database configuration (for docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_USER=myuser
DB_PASSWORD=mysecretpassword
DB_NAME=mydatabase

# Postgres connection URL (used by the app)
DB_URL=postgres://myuser:mysecretpassword@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=disable
# ...existing code...